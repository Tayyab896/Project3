@model IEnumerable<myProperty.Models.AuctionReport>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Auction.AuctionID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WinningBid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinalSalePrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Auction.AuctionID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WinningBid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinalSalePrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReportID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReportID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReportID })
        </td>
    </tr>
}

</table>
